// Fixed issue with variable scope
let myVariable = 10; // Changed from global scope to local

// Improved function to handle edge cases
function performAction(input) {
    if (input < 0) { // Checking for negative input
        console.error('Invalid input');
        return;
    }
    // Core logic remains the same
    console.log(myVariable + input); // Ensured variable is accessible
}